# =============================================================================
# INFOTEC - Configuración Automática de Laravel con Docker Compose
# =============================================================================
# PROPÓSITO: Crear y ejecutar Laravel automáticamente con UN SOLO COMANDO
# USO: docker compose up -d
# RESULTADO: Laravel ejecutándose en http://localhost:8000
# =============================================================================

services:
  # ============================================
  # BASE DE DATOS MARIADB
  # ============================================
  mariadb:
    image: bitnami/mariadb:11.4
    container_name: infotec_mariadb
    environment:
      - ALLOW_EMPTY_PASSWORD=no
      - MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD}
      - MARIADB_DATABASE=${MARIADB_DATABASE}
      - MARIADB_USER=${MARIADB_USER}
      - MARIADB_PASSWORD=${MARIADB_PASSWORD}
      - MARIADB_CHARACTER_SET=utf8mb4
      - MARIADB_COLLATE=utf8mb4_unicode_ci
    volumes:
      - mariadb_data:/bitnami/mariadb
    restart: unless-stopped
    networks:
      - laravel-network
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h localhost --silent || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    security_opt:
      - no-new-privileges:true

  # ============================================
  # APLICACIÓN LARAVEL CON CREACIÓN AUTOMÁTICA
  # ============================================
  laravel:
    image: bitnami/laravel:11
    container_name: infotec_laravel
    environment:
      # Variables específicas de Bitnami
      - LARAVEL_DATABASE_TYPE=mysql
      - LARAVEL_DATABASE_HOST=mariadb
      - LARAVEL_DATABASE_PORT_NUMBER=3306
      - LARAVEL_DATABASE_NAME=${MARIADB_DATABASE}
      - LARAVEL_DATABASE_USER=${MARIADB_USER}
      - LARAVEL_DATABASE_PASSWORD=${MARIADB_PASSWORD}
      - LARAVEL_VERSION=${LARAVEL_VERSION:-11.*}
      
      # Variables para Laravel .env
      - DB_CONNECTION=${DB_CONNECTION:-mysql}
      - DB_HOST=${DB_HOST:-mariadb}
      - DB_PORT=${DB_PORT:-3306}
      - DB_DATABASE=${DB_DATABASE:-${MARIADB_DATABASE}}
      - DB_USERNAME=${DB_USERNAME:-${MARIADB_USER}}
      - DB_PASSWORD=${DB_PASSWORD:-${MARIADB_PASSWORD}}
      
      # Configuración PHP
      - PHP_MEMORY_LIMIT=256M
      - PHP_MAX_EXECUTION_TIME=300
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app
      - vendor_data:/app/vendor
      - composer_cache:/root/.composer/cache
    command: >
      bash -c "
        echo '🚀 INFOTEC - Configuración automática de Laravel'
        
        # Esperar MariaDB usando netcat (más confiable)
        echo '⏳ Esperando MariaDB...'
        for i in {1..30}; do
          if nc -z mariadb 3306 2>/dev/null; then
            echo '✅ MariaDB disponible en puerto 3306'
            break
          fi
          sleep 2
        done
        
        cd /app
        
        # Crear Laravel si no existe
        if [ ! -f artisan ] && [ ! -f composer.json ]; then
          echo '📁 Creando nuevo proyecto Laravel...'
          
          # Preservar .gitignore personalizado
          [ -f .gitignore ] && cp .gitignore /tmp/custom-gitignore
          
          # Crear proyecto Laravel
          composer create-project laravel/laravel:\$$LARAVEL_VERSION /tmp/laravel --prefer-dist --no-interaction
          cp -r /tmp/laravel/. . && rm -rf /tmp/laravel
          
          # Restaurar .gitignore personalizado
          [ -f /tmp/custom-gitignore ] && cp /tmp/custom-gitignore .gitignore
          
          echo '✅ Laravel creado exitosamente'
        fi
        
        # Instalar dependencias
        [ ! -d vendor ] || [ ! -f vendor/autoload.php ] && composer install --no-interaction --optimize-autoloader
        
        # Configurar Laravel
        [ ! -f .env ] && cp .env.example .env
        
        # Configurar base de datos usando variables de entorno
        sed -i \"s/DB_CONNECTION=.*/DB_CONNECTION=\$$DB_CONNECTION/\" .env
        sed -i \"s/DB_HOST=.*/DB_HOST=\$$DB_HOST/\" .env
        sed -i \"s/DB_PORT=.*/DB_PORT=\$$DB_PORT/\" .env
        sed -i \"s/DB_DATABASE=.*/DB_DATABASE=\$$DB_DATABASE/\" .env
        sed -i \"s/DB_USERNAME=.*/DB_USERNAME=\$$DB_USERNAME/\" .env
        sed -i \"s/DB_PASSWORD=.*/DB_PASSWORD=\$$DB_PASSWORD/\" .env
        
        echo '⚙️ Configuración de BD: MariaDB ('"\$$DB_HOST:\$$DB_PORT/\$$DB_DATABASE"')'
        
        # Generar APP_KEY si no existe
        grep -q 'APP_KEY=base64:' .env || php artisan key:generate --force
        
        # Migrar base de datos
        php artisan migrate --force
        
        # CREAR Y CONFIGURAR DIRECTORIOS DE STORAGE (FIX PARA CODESPACES)
        echo '📁 Creando directorios de storage requeridos...'
        mkdir -p storage/framework/{cache,sessions,testing,views}
        mkdir -p storage/framework/cache/data
        mkdir -p storage/logs
        mkdir -p storage/app/{private,public}
        mkdir -p bootstrap/cache
        
        # Crear archivos .gitignore para preservar estructura
        echo '*' > storage/framework/cache/data/.gitignore
        echo '!.gitignore' >> storage/framework/cache/data/.gitignore
        echo '*' > storage/framework/sessions/.gitignore
        echo '!.gitignore' >> storage/framework/sessions/.gitignore
        echo '*' > storage/framework/testing/.gitignore
        echo '!.gitignore' >> storage/framework/testing/.gitignore
        echo '*' > storage/framework/views/.gitignore
        echo '!.gitignore' >> storage/framework/views/.gitignore
        echo '*' > storage/logs/.gitignore
        echo '!.gitignore' >> storage/logs/.gitignore
        echo '*' > bootstrap/cache/.gitignore
        echo '!.gitignore' >> bootstrap/cache/.gitignore
        
        # Configurar permisos (FIX PARA CODESPACES)
        echo '🔐 Configurando permisos de storage...'
        find storage -type d -exec chmod 775 {} \; 2>/dev/null || chmod -R 775 storage
        find storage -type f -exec chmod 664 {} \; 2>/dev/null || chmod -R 664 storage/*/
        find bootstrap/cache -type d -exec chmod 775 {} \; 2>/dev/null || chmod -R 775 bootstrap/cache
        find bootstrap/cache -type f -exec chmod 664 {} \; 2>/dev/null || chmod -R 664 bootstrap/cache/
        
        # Limpiar cache existente para evitar problemas
        echo '🧹 Limpiando cache existente...'
        php artisan config:clear 2>/dev/null || true
        php artisan cache:clear 2>/dev/null || true
        php artisan view:clear 2>/dev/null || true
        
        echo '✅ Directorios de storage configurados correctamente'
        echo '🎉 Laravel listo en http://localhost:8000'
        
        # Iniciar servidor
        exec php artisan serve --host=0.0.0.0 --port=8000
      "
    depends_on:
      mariadb:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - laravel-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s


# =============================================================================
# VOLÚMENES PERSISTENTES
# =============================================================================
volumes:
  mariadb_data:
    driver: local
    # Persistencia de datos de MariaDB (volumen Docker gestionado)
  
  vendor_data:
    driver: local
    # Carpeta vendor de Composer para rendimiento (ESENCIAL en Windows)
    
  composer_cache:
    driver: local
    # Cache de Composer para acelerar instalaciones

# =============================================================================
# RED INTERNA
# =============================================================================
networks:
  laravel-network:
    driver: bridge
    # Red interna para comunicación entre contenedores
    